# Snippets Cheatsheet

## JavaScript

* `cl` = `console.log();`
* `fun` = function
* `if` = if statement
* `ife` = if else statement
* `ter` = tertiary conditional
* `for` = for loop
* `wh` = while loop
* `ret` = return
* `timeout` = timout function
* `switch` = switch case
* `proto` = prototype

## Ruby

* `if`
* `ife` = if else
* `for`
* `while`

### Rails

* `hm` = has many

