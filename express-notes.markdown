In development (so that you don't have to restart the server when making changes to the app.js file), do the following:

  1.  `npm install -g --save-dev nodemon `
  2.  Instead of `node app.js`, use `nodemon app.js`